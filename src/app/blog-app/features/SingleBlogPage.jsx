import { useSelector } from "react-redux"
// import { selectBlogsById } from "./blogSlice"

export default () => {
    // const blog = useSelector(state => selectBlogsById(state, blogId))

    // if(!blog) {
    //     return (
    //         <section>
    //             <h2>blog not found!</h2>
    //         </section>
    //     )
    // }

    return (
        <article>
            {/* <h2>{blog.title}</h2>
            <p>{blog.body}</p> */}
        </article>
    )
}